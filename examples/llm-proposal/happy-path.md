# LLM Proposal — Happy Path

Validate a typed proposal JSON/YAML file, run patch sandbox checks, record
sandbox evidence, then accept the exact diff with validation-run evidence.


Expected result: the scenario produces the documented receipt, report, or blocking finding without bypassing Operation Runtime.
